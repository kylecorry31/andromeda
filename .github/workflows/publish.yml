on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Jitpack build
        run: curl https://jitpack.io/com/github/${{ github.repository	}}/${{ github.event.release.tag_name }}/build.log -m 600
